package ${package}.${modulename}.service ;

import java.util.List;

import com.baomidou.mybatisplus.core.metadata.IPage;
import ${package}.${modulename}.base.IBaseService;
import ${package}.${modulename}.dto.req.${className}Page;
import ${package}.${modulename}.dto.resp.${className}PageResp;
import ${package}.${modulename}.entity.${className};

/**
 * ${comments}服务
 * 
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Service extends BaseService <${className}> {


    /**
     * 分页
     * @param page 分页查询参数
     * @return 分页列表
     */
    IPage<${className}PageResp> page(${className}Page page);

}
